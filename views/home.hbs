
    {{#if removedMovie}}
<div class="alert alert-success alert-dismissible fade show" role="alert">
  Record has been reomved
  <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
</div>
{{/if}}



<div class="row">
  <div class="col-6">
    <h1>Movies Collections</h1>
  </div>
  <div class="col-6 d-flex">
    <a
      href='/add'
      type="button"
      class="btn btn-primary align-self-center"
    >Add Movie</a>
  </div>
</div>

<div class="col">
 <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th scope="col">ID</th>
        <th scope="col">Title</th>
        <th scope="col">Year</th>
        <th scope="col">Actions</th>
      </tr>
    </thead>
    <tbody>
      {{#each rows}}
        <tr>
          <th scope="row">{{this.id}}</th>
          <td>{{this.title}}</td>
          <td>{{this.year}}</td>

          <td>

            <a href="/{{this.id}}" type="button" class="btn btn- btn-small"><i class="bi bi-eye"></i>View</a>
            <a href="/edit/{{this.id}}" type="button" class="btn btn-light btn-small" ><i class="bi bi-pencil"></i>  Edit</a>
            <a href="/remove/{{this.id}}" type="button" class="btn btn-light btn-small"><i  class="bi bi-person-x" ></i> Delete</a>
          </td>
        </tr>

      {{/each}}
    </tbody>
  </table>
</div>